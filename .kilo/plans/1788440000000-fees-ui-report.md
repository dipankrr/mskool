# Fees UI run report (2026-09-04, overnight autonomous)

Live status of the `feature/fees-ui` run against
`.kilo/plans/1788440000000-fees-ui.md`. One line per chunk, newest
last. The plan's Reviewer's guide (in the plan file) holds the per-
commit review entries; this file is the recovery anchor.

## STATUS: RUN STARTING — 0/12 chunks

- Branch `feature/fees-ui` created 2026-09-04 from
  `feature/phase4-fees` @ 8f0b6d4 (backend at 20 commits, S6 committed
  by the owner 00:12). Base gates at start: check-types 8/8, unit 215,
  lint 0 errors.
- The owner-added skills (`ui-ux-pro-max`, `vercel-react-best-
  practices` + `skills-lock.json`) ride with UI0.

---

## Chunk log

### UI0 — `docs: the fees UI plan`
- Status: DONE, committed
- The plan doc (protocol, hard context, locked decisions, 12 chunks,
  ledger, deferrals) + this report skeleton + the owner-added skills.
- Skills recorded: none queried yet (docs-only chunk).

## Dev-server logins for walks (seed)
admin@demo-trust.test / principal@demo-trust.test /
accountant@demo-trust.test / teacher@demo-trust.test — `Password123!`
Web :3000, API :4000. Restart API between smoke runs (sign-in limiter).

### UI1 — `feat(web): fees plumbing — money lib, nav, tabs`
- Status: DONE, committed 23107f2
- money.ts born (41 tests — caught the 1,20,00 grouping bug in my own
  first regex, and the negative-display gap); row types wired;
  copy.fees full vocabulary; nav any-of + five-tab skeleton routes.
- Gates: check-types 8/8; web unit 89; lint 0 errors.
- Walk: accountant → all 5 tabs + /fees→Dues redirect; class_teacher
  → Fees nav with exactly Dues+Payments; subject_teacher → no Fees.
- Walk-tooling note: agent-browser `click @ref` on Sign-out flaky
  (2 attempts no-op); direct `eval` JS click works — use that from
  /profile for role switches. The `wait` command hit os error 10060
  twice on localhost:3000 (dev-server HICcup, not the app — snapshot
  after a sleep instead).
- UI/UX skills queried this chunk: none (structural plumbing, no
  visual decisions beyond the attendance-tab idiom already locked).

### UI2 — `feat(web): the fee heads screen`
- Status: DONE, committed 98bf75e
- Setup tab part 1: heads list + create/edit dialog (RHF on the contract
  schemas) + retire confirm; fee-enums/fee-styles support files.
- Gates: check-types 8/8; web unit 89; lint 0 errors.
- Walk (accountant): Bus Fee (optional) + Exam Fee (one_time) created,
  TUIF edited onto Tuition Fee, duplicate refused w/ dialog held open,
  retire → is_active=false — ALL verified via direct DB query
  (node+postgres one-off in packages/db — psql absent on this host).
- WALK SLIP, recorded honestly: intended to retire Exam Fee, clicked
  Bus Fee's row action (row-order misread). DB check caught it; Bus Fee
  re-activated via SQL (no endpoint — deliberate, hard rule 2 posture);
  Bus Fee is needed active for UI5 anyway. Lesson for later walks:
  match the row's name cell to the action's row before clicking.
- DB tooling note: psql not installed; use the node postgres one-off
  from packages/db (env read from root .env; delete the script after).

### UI3 — `feat(web): fee structures — lines and late-fee rules`
- Status: DONE, committed a32a0ea
- Setup tab complete: structures section (year select w/ history gate),
  create dialog (name auto-suggest), detail page (lines + late-fee rules
  cards). copy.fees.frequencies trimmed — upfront is a structure MODE not
  a line frequency; the exhaustive Record map caught it.
- Gates: check-types 8/8; web unit 89; lint 0 errors (18 pre-existing
  web warnings unchanged).
- Walk (accountant): seeded structure + its ₹12,000 Tuition line render;
  Exam Fee ₹1200.00 line added (DB row verified); invalid 12.505 → the
  contract's own field error (forms-are-the-contracts proof); flat ₹50 /
  15-day rule added (DB row verified).
- Walk-tooling: ref-clicks stayed flaky on links + one of two same-label
  buttons (empty-state duplicate); JS eval clicks are the reliable path.

### UI4 — `feat(web): the student fee profile`
- Status: DONE, committed 342dd78
- The Fees card on the student detail page: assign (no structure select —
  service resolves), generate (idempotent), concession (server-computed
  amount toasted), recompute (windowed, never-paid-only). use-fee-dues
  born early (shared with UI6).
- Gates: check-types 8/8; web unit 89; lint 0 errors.
- Walk (admin): DEMO-0002 assigned (13,200 snapshot incl. the UI3 exam
  line), 2 instalments generated (effective-from past session months →
  one bucket per head — documented generator slicing, NOT a bug),
  10% scholarship → ₹1,320, recompute → 10,800/1,080 nets. ALL DB-verified.
- THE WALK'S BIGGEST FINDING, three suspicion rounds, all cleared:
  1. First recompute applied NOTHING → suspected drizzle date mapping;
     ruled out — the API's own dues response carries ISO STRINGS
     ("2025-06-01"); the raw-SQL Date I saw was MY query bypassing
     drizzle's mapping.
  2. Suspected the window dates themselves → set validFrom via a plain
     DOM value assignment; submitted; still zero → suspected the
     windowedConcessionShares comparison.
  3. Traced the PURE MATHS with the exact fixture inputs (temp vitest
     file, removed) → shares correct (120000/12000 paise). Conclusion:
     the date never reached RHF. RE-LEARNED WALK RULE: native value
     setter + input event, or the field silently keeps its default.
     validFrom had defaulted to TODAY; due dates are 09-03; the S2
     window hardening correctly zeroed the shares.
  - The backend survived unblemished. The covering-window run worked
    first try once the date was set correctly.
- Fixture hygiene: 2 walk-attempt concessions (non-covering windows,
  ₹1,320 each — would consume budget on any future recompute) deleted
  via SQL; the working one kept.
- UI testing has drifted the demo org (DEMO-0002 enrolled+assigned+2
  instalments, Bus/Exam heads, structures line, late-fee rule) — all
  additive, seed is find-or-create; reset:demo → seed restores clean
  when a gate needs it.

### UI5 — `feat(web): optional-fee subscriptions`
- Status: DONE, committed 5bfa966
- Optional services section in the fee profile card: subscribe (optional
  heads only), cancel (confirm + records-kept). One REAL UI bug caught
  by the walk: the subscribe form omitted studentId/academicYearId
  (merged at submit), but the schema REQUIRES them → zodResolver refused
  SILENTLY (no visible field errors — the invalid fields weren't inputs;
  no toast; the dialog just stayed). Fix: context as form defaultValues,
  schema validates whole. Rule recorded: a form whose schema names a
  field must provide that field to RHF, never to a submit-time merge.
- Gates: check-types 8/8; web unit 89; lint 0 errors.
- Walk (accountant, DB-verified): Bus Fee ₹800/mo subscribed (row
  created); Generate → nothing new (window past session end — the
  documented slicing); Cancel → status cancelled, row KEPT (hard rule 2).
- Walk-tooling: Base UI Select opens with pointerdown (not click);
  trigger shows the raw value before the label paints — harmless.

### UI6 — `feat(web): the dues screen`
- Status: DONE, committed 2183555
- Grouped arrears view (client-side grouping of server rows, register
  name join), per-student + grand totals (addMoney), year/student/
  due-by filters, per-row Waive (approve-tier + never-paid mirror).
- money.ts: addMoney now null-tolerant — drizzle-zod infers GENERATED
  money columns as nullable (type-probe proved it); nulls are absent,
  never ₹0.00.
- Gates: check-types 8/8; web unit 89; lint 0 errors (17 warnings,
  below the 18 baseline).
- Walk (DB-verified): accountant sees grouped dues, June partial row
  honestly rendered, ZERO waive buttons; class_teacher read-only;
  principal — June DISABLED (partly paid), waived July DEMO-0001 →
  payment_status 'waived' + waiver_applied ₹1,000 debit ledger row.
- Demo-org drift so far (additive): 2 heads, 1 structure line, 1
  late-fee rule, DEMO-0002 enrolled+assigned+concession, June ₹1
  partial (pre-existing), July waived, 1 cancelled subscription.

### UI7 — `feat(web): the collection counter + receipt`
- Status: DONE, committed 02c7b0c
- The flagship: search → pick student → allocation builder (clamped
  per row, Pay-in-full one-tap, derived read-only total) → mode with
  conditional fields → receipt panel (server's answer verbatim, print,
  link). Idempotency key per attempt, regenerated only on success,
  riding inside the contract data. Broad invalidation after money.
- Gates: check-types 8/8; web unit 89; lint 0 errors (18 baseline).
- Walk (accountant, DB-verified): CASH ₹1,400 (Aug full + Sep 400
  partial) → RCP-2025-00004 cleared + ₹100 server late fee → total
  1,500; DB: row + Aug paid + Sep partial + fee_payment AND
  late_fee_charged ledger rows. CHEQUE Nov → RCP-2025-00006 pending
  ₹1,050, CHQ-778899 in DB. Over-allocation: 5,000 typed → clamped to
  1,000 by the input itself.
- WALK SLIPS, recorded: (1) one misfire — mode select didn't take
  before a submit → RCP-2025-00005 recorded as CASH ₹1,050; left
  deliberately as UI8's reversal exercise. (2) cheque's bankName
  didn't reach the wire (programmatic fill missed the Bank field) —
  walk-tooling gap, the input exists and renders. (3) A disabled
  submit is pointer-events:none — JS .click() no-ops silently; the
  'no network call' confusion was the empty-allocations disable
  working CORRECTLY.
- Selects: the reliable automation path is focus + Enter keydown
  (option list + option click both via keyboard events). Pointer
  paths are flaky in this environment.
- LATE FEE proven live: the UI3-walk rule (flat ₹50/15-day grace)
  charged ₹100 on two overdue instalments — computed server-side,
  frozen on the payment, rendered only on the receipt. The whole
  integrity story of the slice, in one receipt.

### UI8 — `feat(web): payments, transitions, and refunds`
- Status: DONE, committed e1927d2
- Payments list (filters, status tints, mobile cards) + detail page
  with the state machine ENCODED (availableTransitions), reason-required
  transition dialogs, refund dialog with money fields, duties matrix.
- Gates: check-types 8/8; web unit 89; lint 0 errors (18 baseline).
- Walk (DB-verified): principal cleared cheque 00006 (+reason in DB);
  reversed misfire 00005 → October re-opened (paid 1000→0) +
  fee_refund ₹1,000 debit row; late fee ₹50 correctly NOT refunded.
  Accountant: Refund ONLY visible (Clear/Bounce/Reverse hidden) →
  ₹500 UPI refund on seed 00001 → fee_refunds row + debit ledger row.
- Walk artifact: refund dialog automation indexed the Select's hidden
  input, swapping reference/reason values in the DB row — UI fields
  correct, recorded honestly.
- The full transition matrix now has live demo data: pending→cleared
  (00006), cleared→reversed (00005), cleared→refund (00001). Cancel
  + bounce remain exercised by the integration suite (42 fees tests)
  and the E2E (UI10 will add bounce).

### UI9 — `feat(web): the ledger + opening balances`
- Status: DONE, committed 6d6cf82
- The five fee tabs complete. Ledger: read-only by design (filters
  only), In/Out/Net presentation totals. OB section in the fee card:
  record dialog (origin ≠ landing year), status tints, honest
  no-payment-path copy.
- Gates: check-types 8/8; web unit 89; lint 0 errors (15 warnings —
  BELOW the 18 pre-slice baseline; the sweep removed 3).
- Walk (admin, DB-verified): ledger = 12 rows, In 5,601 / Out 2,500 /
  Net 3,101 (the whole slice's money story in one screen); ₹5,000 OB
  for DEMO-0002 (2024-25 → 2025-26) → opening_balances row + paired
  opening_balance credit ledger row.
- Walk-tooling: the admin (two schools) must choose a branch before
  the ledger's year select offers anything — the empty state with no
  branch is honest, but note for future walks: pick MAIN in the
  context sheet first.
- Plan-doc tooling lesson: bash heredoc + node -e ate backticks in
  the reviewer entry (empty code spans); fixed by hand. Later
  entries go in via the Edit tool only.

### UI10 — `test(e2e): the counter flow — and two UI bugs it caught`
- Status: DONE, committed b70f12b
- 3 durable tests (cash receipt, cheque pending, admin bounce → dues
  re-open) + accountant auth state in global-setup + flows.spec MARK_DATE
  bump (12-08 consumed → 12-15) and a conditional roster assertion (the
  reset demo org ships 1-student section A; the old unconditional
  "rest read Present" failed on it).
- THE ARMOR CAUGHT TWO REAL UI BUGS before landing:
  1. Counter empty-state race: "Nothing to collect" rendered from a
     not-yet-enabled dues query for ~1s when the session resolved after
     the cashier's pick. Fixed: empty state gates on isSuccess; honest
     loading interim. (Found by time-slicing the panel state in an
     isolated playwright script: t+500 nothing=true, t+1500 rows=11.)
  2. Bounce offered only on CLEARED — the service allows pending OR
     cleared (bounced-on-first-presentation is the classic case).
     availableTransitions now mirrors transitionPayment exactly.
- E2E debugging journey, recorded for the next agent: repeated suite runs
  DRAINED the demo dues (spec pays full installments) → the drain-guard
  message fired → reset:demo → db:seed restored the fixture (the plan's
  documented path; smoke should re-run before a clean gate needs it).
  The 429 sign-in limiter forced API restarts between suite runs (twice).
  Playwright quirks: .or() needs .first() in strict mode; Base UI's
  branch-select portal leaves an inert overlay (Escape +
  detach-wait on the presentation div only — the shell's own
  data-base-ui-inert is permanent); the counter's student picker
  re-renders rows on debounce (wait for the filtered row first).
- FINAL E2E GATE: pnpm test:e2e — 12 passed (routes, flows,
  session-boundary, fees-counter) on the fresh fixture.
- Other gates: check-types 8/8; web unit 89; lint 0 errors (15 warnings,
  below the 18 pre-slice baseline).

### Run summary at UI10 close
- Branch feature/fees-ui: 10 commits (UI0 plan+skills, UI1 plumbing,
  UI2 heads, UI3 structures, UI4 profile, UI5 subscriptions, UI6 dues,
  UI7 counter, UI8 payments, UI9 ledger, UI10 E2E — that's 11 including
  this chunk's fixes inside UI10's own commit).
- Every chunk walked in a real browser with DB verification; every
  mutation verified against the database, never the toast.
- The three rules held throughout: forms ARE the contracts (the money
  regex as field errors, twice proven); the UI never computes what the
  server owns (derived totals, server-computed concessions, frozen late
  fees on receipts); the state machine encoded in the UI (illegal
  transitions do not render — corrected once by the E2E against the
  service's own preconditions).

### UI11 — `docs: TASKS.md — the fees UI milestone done`
- Status: DONE (this commit)
- TASKS.md resume-here rewritten: two review piles (backend 20 +
  UI 11 commits), merge order (backend first, UI rebases clean —
  apps/web-only), the full verification surface, deferrals restated,
  next-up options (Phase 5 exams primary).
- Plan UI11 boxes ticked; UI10 + UI11 reviewer entries in place.
- FINAL FULL GATE (all on the closing fixture, all green):
  - check-types 8/8
  - unit 256 (86 authz + 49 services + 32 trpc + 89 web)
  - integration 135 (93 authz + 42 fees)
  - db:verify 119 PASS
  - check:builders + check:openapi green (100 endpoints, 36 fee —
    unchanged; the UI touches no backend)
  - lint 0 errors (web warnings 18 → 15, BELOW the pre-slice baseline)
  - test:e2e 12 passed (routes, flows, session-boundary, fees-counter)
  - smoke:authz 172/172 (after reset:demo → seed; the FINAL E2E run
    after the last reset re-drifts the demo org — the documented
    pattern; reset:demo → seed → smoke for a clean gate on demand)

## RUN COMPLETE — 12/12 chunks, 12 commits, feature/fees-ui

Everything the plan promised shipped; nothing was skipped, hidden, or
silently deferred. Two real UI bugs were caught by the E2E armor and
fixed in the commit that added it. The backend survived every walk
unblemished (three suspicion rounds in UI4, all cleared). One pre-
existing E2E fragility (the marking flow's roster assumption) was
repaired alongside. The demo org was reset + re-seeded twice (the
documented fixture path) as the UI's testing consumed its dues.

The owner's morning review: `git log feature/phase4-fees..feature/fees-ui`
— 12 commits, each with its Reviewer's-guide entry in the plan. No
pushes. No main commits. The branch waits.
