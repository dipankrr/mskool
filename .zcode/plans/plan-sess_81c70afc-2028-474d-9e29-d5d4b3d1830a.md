# Implement `.kilo/plans/1787537595000-authz-api-shape-refactor.md.md` as written

The detailed plan file (written by the other agent, fully reviewed and adjudicated in this conversation) is the source of truth. This plan covers how I execute it.

## Protocol (from the plan file, followed exactly)
One chunk per turn: implement → run the chunk's Verify steps → tick its boxes in the plan file → `git status` / `git diff --stat` summary → propose the commit message verbatim → **stop**. The user reviews and commits; I never run git add/commit/push. Every chunk leaves `pnpm check-types` green and the app bootable.

## Execution order (per the file's dependency graph)
**A2 first** (the net), then A3, then A1/A4/A5/A6 in any order, B1/B2 (low risk), **B3 = the ADRs — hard stop for owner accept/reject before B4–B7**, then B4→B5 and B6→B7(non-node) with B7(node) after B3 only, C any time. Deadlines: B6 before the enrollment slice, B7 before the first subject-teacher screen.

## First chunk — A2, in detail
1. **Seed prerequisite** (`apps/api/scripts/seed.ts`): after `classA`, create one section under Class 6 in school A's current session **through the section service** (hard rule 12 — `scope_nodes` row in the same transaction); create a `subject_teacher` user via the existing find-or-create helper; grant `findOrCreateAssignment(..., "subject_teacher", "section", sectionA.id, adminUser.id)`; add her to the `invalidateUserAuthCache` block and the printed login summary.
2. **API half**: extend `apps/api/scripts/smoke-authz.ts` with the roles × procedures baseline matrix (four logins × me.get / school.list / year.list / year.current / class.list / class.byId / section.list + one mutation each), pinning expected 200/403/404.
3. **Browser half**: add `@playwright/test` to `apps/web`; one spec under `e2e/` iterating the four logins × every route, asserting no permission-error text, no console error, non-empty `main`; add `auth-subjectteacher.json`; wire `test:e2e` as its own script (not behind `pnpm test`); keep vitest and Playwright globs disjoint.
4. **Verify**: `pnpm db:seed` idempotent on second run; `pnpm smoke:authz` green with the matrix; `pnpm test:e2e` green for all four logins; the three per-fix reverts of `d8eca60` (useClass rewrite alone → class_teacher class-detail assertion fails; nav filter alone → nav assertion fails; enabled gate alone → console-error assertion fails) each make the suite fail. Then tick A2's boxes and stop for review.

## Known deviations to surface when reached
- **B2's "frontend fallout" note is moot**: the client never calls `year.current` (it resolves sessions from `year.list` per `active-context.tsx:54-61`), so B2 needs no client change today — I'll confirm and note it in that chunk's summary rather than inventing work.
- If any Verify step fails for environmental reasons (e.g., Playwright/browser install on Windows), stop and report rather than leaving a broken intermediate state, per the protocol.

## Gates on every chunk
`pnpm check-types` green (8/8) · `pnpm test` green (54 authz runtime count — do not "correct" to 42) · `pnpm test:e2e` green once A2 lands · `pnpm smoke:authz` extended with the chunk's negatives · `pnpm check:openapi` clean when router meta changes · TASKS.md updated per the working agreement.