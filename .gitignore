node_modules
node_modules/
dist
.next
.turbo
*.tsbuildinfo
.env
.env.*
!.env.example
.DS_Store
rough.md
*.log

# playwright-cli scratch: page snapshots and console logs.
.playwright-cli/
# Saved browser auth state per seeded role. Holds a LIVE session cookie —
# committing one hands over a working login. Root-anchored so it cannot swallow
# anything inside packages/auth.
/auth-*.json

# Playwright artifacts (screenshots/traces from failed e2e runs).
test-results/
playwright-report/

# Agent skills are installed to disk but never committed — 157k lines of vendored
# data were once swept into a feature branch and had to be filtered out of history.
.agents/skills/
.zcode/skills/
