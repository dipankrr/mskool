See [AGENTS.md](./AGENTS.md).

That file is the single entry point for all AI coding agents in this repo — stack,
commands, hard rules, and pointers into `docs/`. This file exists only so Claude Code
finds its way there.
