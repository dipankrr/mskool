// Business logic lives here — classes + exported singletons, no HTTP awareness.
// Domain services land here as each phase ships; see docs/TASKS.md.
// The empty export keeps this a module until the first service arrives.
export {};
